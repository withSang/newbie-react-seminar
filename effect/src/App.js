import React from 'react';
import './App.css';
import Feed from './Feed';

// call `await api()` to get data
import api from './api';

const App = () => {
  return (
    <div className="App">
      <h1>Feed</h1>
      {/* Show loading or feeds */}
    </div>
  );
};

export default App;
